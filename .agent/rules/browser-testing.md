---
trigger: always_on
---

# Browser Testing Rules

* Always execute browser tests and UI verifications on the `simpleshop.local` domain (e.g., `http://demo.simpleshop.local:3000`), unless explicitly instructed otherwise by the user. Do not default to `localhost`.
